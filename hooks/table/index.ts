/**
 * Shared Table Hooks
 *
 * These hooks provide reusable logic for table functionality:
 * - Data fetching from Redux
 * - Dropdown menu state management
 * - Client-side sorting
 *
 * Usage:
 * import { useTableData, useDropdownMenu, useTableSort } from "@/hooks/table"
 */

export { useTableData } from "./useTableData";
export { useDropdownMenu } from "./useDropdownMenu";
export { useTableSort } from "./useTableSort";
