export interface Feature {
  id: string;
  title: string;
  description: string;
  icon: string;
}
export interface TrustedByItem {
  id: string;
  name: string;
  icon: string;
  description: string;
}
